let person="Steve Irwin";

function changeTitle ( ) {let title=
document.getElementById("title"); textContent = person;
if (title.textContent=="Ledgend") {
title.textContent=person;
}else
{
title.textContent="Ledgend";
}
}